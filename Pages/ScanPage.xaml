<Page x:Class="HPPrinterScanner.Pages.ScanPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Scan Network">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Install a Printer"
                   FontSize="20" FontWeight="SemiBold" Margin="0 0 0 12" />

        <!-- Controls row -->
        <Grid Grid.Row="1" Margin="0 0 0 12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*"    />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:Button x:Name="ScanButton" Grid.Column="0"
                       Content="Scan" Icon="{ui:SymbolIcon Symbol=Search24}"
                       Appearance="Secondary"
                       Click="ScanButton_Click"
                       Margin="0 0 8 0" />
            <ui:Button x:Name="CancelButton" Grid.Column="1"
                       Content="Cancel"
                       IsEnabled="False"
                       Click="CancelButton_Click" />
            <TextBlock x:Name="SubnetLabel" Grid.Column="2"
                       VerticalAlignment="Center"
                       Margin="16 0 0 0"
                       Opacity="0.6" FontSize="12" />
        </Grid>

        <!-- Progress -->
        <Grid Grid.Row="2" Margin="0 0 0 12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ProgressBar x:Name="ScanProgress" Grid.Column="0"
                         Minimum="0" Maximum="254" Value="0"
                         Height="8" VerticalAlignment="Center" />
            <TextBlock x:Name="ProgressLabel" Grid.Column="1"
                       Margin="8 0 0 0" VerticalAlignment="Center"
                       Text="0 / 254" />
        </Grid>

        <!-- Results -->
        <DataGrid x:Name="ResultsGrid" Grid.Row="3"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserReorderColumns="False"
                  CanUserSortColumns="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  ColumnHeaderStyle="{StaticResource HoverColumnHeaderStyle}"
                  SelectionChanged="ResultsGrid_SelectionChanged"
                  MouseDoubleClick="ResultsGrid_MouseDoubleClick">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Cursor"     Value="Hand" />
                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Install printer"
                              Icon="{ui:SymbolIcon Symbol=Add24}"
                              Click="InstallButton_Click" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="IP Address"  Binding="{Binding IpAddress}"  Width="120" />
                <DataGridTextColumn Header="MAC Address" Binding="{Binding MacAddress}"  Width="145" />
                <DataGridTextColumn Header="Hostname"    Binding="{Binding Hostname}"    Width="160" />
                <DataGridTextColumn Header="Model"       Binding="{Binding Model}"       Width="*"   />
                <DataGridTextColumn Header="Ports"       Binding="{Binding Ports}"       Width="90"  />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bottom action bar -->
        <Border Grid.Row="4" Margin="-16 8 -16 -16" Padding="16 10"
                BorderThickness="0 1 0 0"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}">
            <ui:Button x:Name="InstallButton"
                       Content="Install Printer" Icon="{ui:SymbolIcon Symbol=Add24}"
                       Appearance="Primary"
                       IsEnabled="False"
                       HorizontalAlignment="Right"
                       Click="InstallButton_Click" />
        </Border>
    </Grid>
</Page>
